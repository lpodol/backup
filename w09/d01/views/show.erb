<html>
  <header>
    <h1>Petulance</h1>
      <a href = "/pets">go home</a>
  </header>
  <body>
    <p><%= thispet[:name] %> is a <%= thispet[:type] %>.</p>
    <h3>Change <%= thispet[:name] %>'s name here:</h3>
      <form action = "/pet/<%=thispet[:id]%>" method = "post">
        <input type="hidden" name="_method" value = "put"> <!--method override. browsers can't handle post or delete yet.-->
        <input type="text" name="newname" placeholder="new name?" required>
        <button>UPDATE IT!</button>
      </form>

      <p><%= thispet[:name] %> can be removed here: </p>
      <form action = "/pet/<%=thispet[:id] %>" method = "post"> <!--method override-->
        <input type="hidden" name="_method" value = "delete">
        <button>GET IT ADOPTED!</button>
      </form>
  </body>
</html>
